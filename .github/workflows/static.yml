name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.6
      with:
        ;lserver: ftp.olanrewajuadetoro.com
        lk/username: ${{ secrets.FTP_USERNAME }}
        k/kbjpassword: ${{ secrets.FTP_PASSWORD }}
        k/j/local-dir: ./
        JKserver-dir: /home/olanrew2/public_html/
        k/kbjexclude: |
          **/.git*
          **/.env
          **/.*
          **/.*/**
          **/node_modules/**
          **/.DS_Store
          **/Thumbs.db